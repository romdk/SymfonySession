{% extends 'base.html.twig' %}

{% block title %}Détails de la formation{% endblock %}

{% block body %}
<main id="detailMain">
    <h1>{{ formation.nomFormation }}</h1>

    <div class="selecteur">
        {# {{ today }} <br> #}
        <span id="currentBtn" class="btnActive">Sessions en cours</span>
        <span id="upcomingBtn">Sessions à venir</span>
        <span id="pastBtn">Sessions passées</span>
    </div>
    <div class="tables">
        <table id="currentSession">
            <tr>
                <th>Statut</th>
                <th>Formation</th>
                <th>Session</th>
                <th>Référent</th>
                <th>Date de début</th>
                <th>Date de fin</th>
                <th>Inscrits</th>
                <th>Actions</th>
            </tr>
            {% for session in sessions %}
            <tr>
                <td> 
                    {% if today > session.dateDebut and today < session.dateFin %}
                    <span>En cours</span>
                    {% elseif today > session.dateFin  %}
                    <span>Terminée</span>
                    {% elseif today < session.dateDebut and session.nbPlacesLibres == 0 %}
                    <span>Complet</span>
                    {% elseif today < session.dateDebut and session.nbPlacesLibres > 0 %}
                    <span>Disponible</span>   
                    {% endif %}
                </td>
                {# <td>{{ session.nomFormation }}</td> #}
                <td>{{ session.nomSession }}</td>
                {# <td>{{ session.prenomReferent }} {{ session.nomReferent }}</td> #}
                <td>{{ session.dateDebut | date('d/m/Y') }}</td>
                <td>{{ session.dateFin | date('d/m/Y')  }}</td>
                <td>{{ session.nbPlacesReserves }}/{{ session.nbPlacesTotal }}</td>
                <td><i class="fa-solid fa-arrow-up-right-from-square"></i></td>
            </tr>                
            {% endfor %}
        </table>           
        <table id="upcomingSession">
            <tr>
                <th>Statut</th>
                <th>Formation</th>
                <th>Session</th>
                <th>Référent</th>
                <th>Date de début</th>
                <th>Date de fin</th>
                <th>Inscrits</th>
                <th>Actions</th>
            </tr>
            {% for session in sessions %}
            <tr>
                <td> 
                    {% if today > session.dateDebut and today < session.dateFin %}
                    <span>En cours</span>
                    {% elseif today > session.dateFin  %}
                    <span>Terminée</span>
                    {% elseif today < session.dateDebut and session.nbPlacesLibres == 0 %}
                    <span>Complet</span>
                    {% elseif today < session.dateDebut and session.nbPlacesLibres > 0 %}
                    <span>Disponible</span>   
                    {% endif %}
                </td>
                {# <td>{{ session.nomFormation }}</td> #}
                <td>{{ session.nomSession }}</td>
                {# <td>{{ session.prenomReferent }} {{ session.nomReferent }}</td> #}
                <td>{{ session.dateDebut | date('d/m/Y') }}</td>
                <td>{{ session.dateFin | date('d/m/Y')  }}</td>
                <td>{{ session.nbPlacesReserves }}/{{ session.nbPlacesTotal }}</td>
                <td><i class="fa-solid fa-arrow-up-right-from-square"></i></td>
            </tr>                
            {% endfor %}
        </table>           
        <table id="pastSession">
            <tr>
                <th>Statut</th>
                <th>Formation</th>
                <th>Session</th>
                <th>Référent</th>
                <th>Date de début</th>
                <th>Date de fin</th>
                <th>Inscrits</th>
                <th>Actions</th>
            </tr>
            {% for session in sessions %}
            <tr>
                <td> 
                    {% if today > session.dateDebut and today < session.dateFin %}
                    <span>En cours</span>
                    {% elseif today > session.dateFin  %}
                    <span>Terminée</span>
                    {% elseif today < session.dateDebut and session.nbPlacesLibres == 0 %}
                    <span>Complet</span>
                    {% elseif today < session.dateDebut and session.nbPlacesLibres > 0 %}
                    <span>Disponible</span>   
                    {% endif %}
                </td>
                {# <td>{{ session.nomFormation }}</td> #}
                <td>{{ session.nomSession }}</td>
                {# <td>{{ session.prenomReferent }} {{ session.nomReferent }}</td> #}
                <td>{{ session.dateDebut | date('d/m/Y') }}</td>
                <td>{{ session.dateFin | date('d/m/Y')  }}</td>
                <td>{{ session.nbPlacesReserves }}/{{ session.nbPlacesTotal }}</td>
                <td><i class="fa-solid fa-arrow-up-right-from-square"></i></td>
            </tr>                
            {% endfor %}
        </table>
    </div>       
</main>
{% endblock %}
